# Dataset Changelog

Versioning rule (docs/04 §4): `vX.Y` — X = major (class set changes), Y = minor (annotations / bug fixes).
Every release must list class counts, source datasets, and any taxonomy delta.

## v0.0 (placeholder)
- Empty. First real release will be `v1.0` after Phase 2 W5.
